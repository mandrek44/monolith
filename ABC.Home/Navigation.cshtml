@* VirtualPath: ~/Home/Home/Navigation.cshtml *@
@using System.Web.Optimization
@using ABC.Infrastructure.Web.Defaults
@model ABC.Home.HomeController.Model

<div class="menu">
    <ul>
        @foreach (var menuItem in @Model.MenuItems)
        {
            <li>@Html.ActionLink(menuItem.MenuLink)</li>
        }
    </ul>
</div>
